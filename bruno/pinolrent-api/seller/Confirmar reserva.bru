meta {
  name: Confirmar reserva
  type: http
  seq: 18
}

patch {
  url: {{baseUrl}}/seller/reservations/{{reservationId}}/confirm
  body: none
}

headers {
  Authorization: Bearer {{sellerToken}}
}

assert {
  $res.status: eq 200
  $res.body.status: isDefined
}