meta {
  name: Registrar pago rechazo
  type: http
  seq: 25
}

post {
  url: {{baseUrl}}/reservations/{{rejectionReservationId}}/payment
  body: json
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{buyerToken}}
}

body:json {
  {
    "method": "cash"
  }
}

assert {
  res.status: eq 201
}
