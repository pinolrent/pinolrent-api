meta {
  name: Registrar pago
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/reservations/{{reservationId}}/payment
  body: json
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{buyerToken}}
}

body:json {
  {
    "method": "pos",
    "proof_url": "https://example.com/recibo.jpg"
  }
}