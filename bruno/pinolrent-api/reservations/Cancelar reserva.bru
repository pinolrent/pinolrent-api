meta {
  name: Cancelar reserva
  type: http
  seq: 16
}

patch {
  url: {{baseUrl}}/reservations/{{reservationId}}/cancel
  body: none
}

headers {
  Authorization: Bearer {{buyerToken}}
}

assert {
  $res.status: eq 409
}